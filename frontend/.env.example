# Frontend environment variables
# ================================
# Copy to .env.local — Vite reads this automatically.

# Backend API URL.
# Leave blank to use the Vite dev proxy (localhost:8000).
# Set to your Render.com backend URL in Vercel environment variables.
VITE_API_URL=http://localhost:8001
